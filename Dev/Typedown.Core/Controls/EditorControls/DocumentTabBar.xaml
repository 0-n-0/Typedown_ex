<UserControl
    x:Class="Typedown.Core.Controls.DocumentTabBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.Core.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Typedown.Core.ViewModels"
    xmlns:models="using:Typedown.Core.Models"
    xmlns:converters="using:Typedown.Core.Converters"
    d:DataContext="{d:DesignInstance Type=viewmodels:AppViewModel}"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    mc:Ignorable="d"
    d:DesignHeight="36"
    d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToObjectConverter x:Key="TabOpacityConverter" TrueValue="1" FalseValue="0.68"/>
        <Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondary}"/>
        </Style>
        <DataTemplate x:Key="DocumentTabTemplate" x:DataType="models:DocumentTab">
            <Grid
                MinWidth="120"
                MaxWidth="220"
                Height="32"
                Padding="12,0,4,0"
                Margin="2,3,0,0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                Opacity="{Binding IsSelected, Converter={StaticResource TabOpacityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    Text="{x:Bind DisplayTitle, Mode=OneWay}"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap"/>
                <Button
                    Grid.Column="1"
                    Style="{StaticResource TabCloseButtonStyle}"
                    Tag="{Binding}"
                    Tapped="OnCloseTabTapped">
                    <FontIcon Glyph="&#xE711;" FontSize="10"/>
                </Button>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="ThisControl" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource CardStrokeColorDefault}" Background="{ThemeResource SolidBackgroundFillColorTertiary}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" HorizontalScrollMode="Enabled" VerticalScrollMode="Disabled">
            <ListView
                x:Name="TabsList"
                ItemsSource="{x:Bind ViewModel.DocumentTabsViewModel.Tabs, Mode=OneWay}"
                ItemTemplate="{StaticResource DocumentTabTemplate}"
                SelectionMode="None"
                IsItemClickEnabled="True"
                ItemClick="OnTabItemClick"
                ScrollViewer.HorizontalScrollMode="Disabled"
                ScrollViewer.VerticalScrollMode="Disabled">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </ScrollViewer>
        <Button
            Grid.Column="1"
            Width="32"
            Height="32"
            Margin="4,2,6,2"
            Padding="0"
            BorderThickness="0"
            Background="Transparent"
            Command="{x:Bind ViewModel.DocumentTabsViewModel.NewTabCommand}">
            <FontIcon Glyph="&#xE710;" FontSize="14"/>
        </Button>
    </Grid>
</UserControl>
